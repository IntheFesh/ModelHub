# ModelHub · Capacity Plan

Generated by `scripts/render_docs.py` from `configs/serve/model_profiles/*.yaml` + `configs/bench/capacity_planning.yaml` — never hand-edited.

**★ PLANNING ESTIMATE, not a measured value.** Every number below is a pure function of `weight_bytes`/`gdn_fixed_state_bytes`, both documented estimates in the source config files (see their own comments) pending confirmation from a real served model's `vLLM` startup log (`serve/vllm_log_parser.py`).

| prompt tokens | Arctic-7B max concurrency | Qwen3.5-9B max concurrency |
|---|---|---|
| 1000 | 175 | 159 |
| 2000 | 97 | 103 |
| 3000 | 67 | 76 |
| 5000 | 41 | 50 |
| 8000 | 26 | 33 |
| 16000 | 13 | 17 |

