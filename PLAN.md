# ModelHub · 执行计划

> 合并此前的 ROADMAP / PLAN-5DAY v1–v2 / AUDIT-v3 / PLAN-v4-layered。
> 那些文件已归档，**排程以本文件为准**。
> 配套：`CLAUDE.md`（约束）、`CLAUDE-CODE-PROMPTS.md`（提示词）、
> `FACTS.md`（事实登记）、`MODEL-SELECTION-FINAL.md`（模型定稿）、`preflight.py`。

---

## 一、目标与判据

**这是找工作的工业项目，不是科研项目。**
判据是面试官问「单卡多少 QPS」「GPU 利用率多少」「怎么上线」「出问题怎么回滚」
「单请求多少钱」时，你答得上来且有 artifact 支撑。

**核心价值不在模型准确率，在平台工程。** 模型层直接用开源 SOTA，
你的价值在网关、门禁、灰度、容量规划、成本模型——那部分开源不提供。

---

## 二、两条道，两台机器

| 道 | 机器 | 跑什么 | 租期 |
|---|---|---|---|
| **A · 推理** | 1× RTX 5090 | 服务 / 网关 / 监控 / **压测（独占）** / 门禁灰度 | D0–D4，4 天 |
| **B · 训练** | 2× A100-80G | SFT / DPO / GRPO / 微调与分布式对比 | D0 晚–D2，2 天 |
| **C · 批处理**（可选） | 1× RTX 5090 | 量化 / 评估批 | D0–D3 |

**为什么训练不放 5090**：sm_120 训练栈有六项已记录故障
（cuBLAS 缺 kernel、PTX JIT 全废、Triton 伪 OOM、bnb 符号错、
双 Blackwell backward 驱动崩溃、编译缓存污染，详见 `FACTS.md`）。
租 A100 不是为了快，是为了**不出意外**，且顺带让 9B 全参微调可行、
ZeRO/FSDP 能真跑。成本约 45 实例小时。

**为什么两台机器是关键**：两条线彻底并行，
「五个通宵是五个不可回收槽位」这个约束消失，L0 从 T+2.5 压到 T+2。

**跨机依赖只有三条，全是小文件**：坏模型 adapter（租→本地）、
比对器+沙箱代码（本地→租）、SFT adapter（租→本地）。
★ 只传 adapter（~200MB），不传合并权重。数据各机从源下载。

---

## 三、分层交付（每层独立可停止）

### L0 · T+2 ★ 最快交付点

| 你手上有 | |
|---|---|
| 六个核心数字 | 单卡 QPS / P99 / MFU-MBU / 瓶颈定位 / 单请求成本 / API crossover 点 |
| 比对器 | 黄金集 150 对 100% 通过 + 与官方脚本一致率 + 不一致 case 人工复核 |
| 平台 | FastAPI 网关（鉴权/限流/配额/路由/熔断）+ Redis + Prometheus/Grafana |
| LLMOps 闭环 | 五道闸门禁 + 灰度 + 自动回滚 + **≥3 条真实拦截 + 1 次真实回滚** |
| 多模型 | Arctic-7B + Qwen3.5-9B 双架构，**KV 交叉曲线实测** |
| 量化 | AWQ / GPTQ / FP8 三方对比 |

**到这里停手，项目已经成立，简历主体全部可写。**

### L1 · T+3 → 买推理优化
前缀缓存命中率与交错度衰减、MTP vs n-gram 推测解码、XGrammar 约束解码、
SGLang 对比、MFU/MBU 架构对照。**AI Infra / 推理优化岗性价比最高的一天。**

### L2 · T+4 → 买训练侧 JD 覆盖
SFT LoRA 上线版、DPO、LoRA/QLoRA/全参、ZeRO/FSDP、Liger/FA/packing。
**算法工程师岗的关键词密度靠这天。**

### L3 · T+5 → 买 RL 与打磨
veRL GRPO、面试稿打磨、demo 脚本。
**强化学习岗不能省；纯 Infra 岗可以不要。**

---

## 四、排程

### Day 0（半天准备 + 一个通宵）

**A 道白天**
- `[下载]` BIRD 33.4GB + Spider + Mini-Dev V2 三方言版，**最先启动**
- 环境：原生 Linux（**不要 WSL2**）/ CUDA 12.8+ / vLLM ≥0.17 / FlashInfer backend
- **跑 `preflight.py`**（两遍：装栈前看 A 段，装栈后跑完整版 `--with-model`）
- 按 preflight 结果决定：Qwen3.5 线放本地还是移到 B 道
- 环境固化 Dockerfile，digest 进 manifest
- `[GPU 1h]` 冒烟 + Arctic-7B 快评基线

**B 道白天**：开机配环境（成熟栈，1–2h）+ 下载数据 + 跑 preflight

**N0 夜间队列**（风险从低到高，`;` 分隔，watchdog 接力，超配 30–50%）

| A 道 | B 道 |
|---|---|
| 1. 双模型全评基线 + gold 结果缓存 (2h) | 1. **B1 SFT 烟测 50 步** (0.5h) |
| 2. Arctic 量化三件套 (5h) | 2. **B1 SFT 长跑**（时间预算封顶） |
| 3. ── 超配 ── | 3. **B3 坏模型 ckpt-B/D** (2h) |
| 4. schema 格式对比 | 4. ── 超配 ── |

★ **坏模型放 N0 是关键改动**：门禁演练 D2 就能做，L0 从 T+3.5 提前到 T+2。
ckpt-A（欠拟合）直接取 SFT 主跑的早期 checkpoint，零成本。

---

### Day 1 · 信任根

| 时段 | A 道 | B 道 |
|---|---|---|
| 上午 | Claude Code 写 A0–A4 | **B2** 六组短跑对比 |
| 上午 | `[人工 2.5h]` **比对器黄金集 150 对裁定** ← 唯一不可外包 | |
| 下午 | `[GPU 0.5h]` 一致率报告（复用 N0 缓存） | |
| 下午 | `[人工 1h]` 过不一致 case，判定谁对 | |
| 下午 | 写 A5 服务层 + prompt/schema | |
| **N1** | 队列：schema 格式对比 + 全评 | 队列：**B5 GRPO**（10 步烟测后挂） |

---

### Day 2 · 平台闭环 → **L0 达成**

| 时段 | A 道 | GPU |
|---|---|---|
| 上午 | 写 A6 网关 + A7 监控 | — |
| 上午 | **A8 二维压测**（并发 × prompt 长度，独占） | 2.5h |
| 上午 | 交叉曲线分析 → **路由阈值回填 configs/gateway/** | — |
| 上午 | 容量规划 + 成本模型 + crossover 点 | — |
| 下午 | 写 A9 门禁 + A10 灰度（fixture 测） | — |
| 下午 | **门禁演练**：ckpt-A/B/D 被不同闸拦下 | 2h |
| 下午 | **灰度 5% → 注入劣化 → 自动回滚** | 1h |
| 傍晚 | `[人工 1.5h]` incident-log，每条讲清根因 | — |

**B 道**：B4 DPO → 打包全部 adapter 回传 → **退机**

**Day 2 结束 = L0。到这里停手项目已经成立。**

---

### Day 3 · 推理优化 → **L1**

| 内容 | GPU |
|---|---|
| A11-2 前缀缓存 + 交错度衰减 + 双架构对照 | 2h |
| A11-3 推测解码 MTP vs n-gram × 并发扫描 | 2h |
| A11-4 XGrammar 约束解码 | 1.5h |
| A11-5 SGLang 对比压测 | 1.5h |
| A11-6 MFU/MBU 架构对照 | 1h |
| `[人工 1.5h]` 追问答一遍 + 补决策记录 | — |

---

### Day 3.5–4 · 收尾 → **全栈**

SFT/DPO 模型走第二轮完整门禁灰度（1h）→ 终版全评与压测（1h）→
A12 收尾 → `[人工 3h]` 架构图 + interview-qa + demo + 八条自检

---

## 五、GPU 预算

| 道 | 项 | 小时 |
|---|---|---|
| A | 环境冒烟 + 三次评估基线 | 4 |
| A | 二维压测 sweep（并发 × prompt 长度 × 2 模型） | 4 |
| A | 量化三件套 + 评估 | 6 |
| A | 前缀缓存 / 推测解码 / XGrammar / SGLang / MFU-MBU | 8 |
| A | 门禁演练 + 灰度 + 终版压测 | 4 |
| A | **小计** | **26** |
| B | SFT 收敛跑（时间预算封顶） | 4–6 |
| B | 微调六组短跑 + ZeRO/FSDP | 7 |
| B | 坏模型 ×2 + DPO + GRPO 短跑 | 9 |
| B | **小计（实例小时 ×2 卡）** | **20–22** |

加 30% 调试余量。A 道租 4 天 / B 道租 2 天，容量宽裕。

---

## 六、四条不可违反的操作纪律

1. **压测机独占。** 启动前 `nvidia-smi` 检查，有第二个进程就拒绝跑。
   共享 GPU 的数字是"看起来正常的假数据"。
2. **过夜任务必先烟测。** 20–50 步验证显存/checkpoint/resume 三项，
   任一失败禁止挂长跑。半小时成本保一个通宵。
3. **夜间队列必须超配 + watchdog。** 夜晚是唯一不可回收的资源，
   白天能加班，空转的通宵找不回来。
   **决策顺序：先决定今晚跑什么，再决定明天做什么。**
4. **未测 ≠ 通过。** 任何跳过、依赖缺失、无法验证的检查项，
   一律不得计为通过。判定用白名单不用黑名单。

---

## 七、每晚 21:00 go / no-go

| 检查 | 通过 | 不通过 |
|---|---|---|
| D0 | preflight 无 CRITICAL 失败；GDN 决策已定 | 当晚解决，不解决不进 D1 |
| D1 | 黄金集 100% 通过、一致率报告已出、B1 烟测过 | 烟测不过 → 放弃当晚长跑，改跑量化 |
| D2 | **六个核心数字 + 交叉曲线 + 路由阈值全部到手** | 没到手 → 砍 D3 全部推理优化，D3 补压测 |
| D3 | 推理优化 ≥4 组完成 | 砍 GRPO，保收尾 |
| D4 | 占位符清零、追问答得上 | **答不上就砍掉那项技术不写进简历** |

**砍项优先级（从先砍到最后砍）**
```
TensorRT-LLM → GRPO → 约束解码 → 推测解码 → SGLang
  → DPO → 微调方式对比 → 量化 → 前缀缓存
  → 【以下不可砍】压测容量规划 / 门禁灰度 / 比对器 / 网关监控
```
**最后四项是这个项目区别于「我部署了个 vLLM」的全部理由。**

---

## 八、成功率判定

| 交付层 | 时点 | 成功率 | 主要残余风险 |
|---|---|---|---|
| L0 | T+2 | **≥95%** | vLLM 起不来（preflight D1 兜底） |
| L1 | T+3 | **~90%** | 单项实验失败可独立砍，不连锁 |
| 全栈 L2+L3 | T+3.5–4 | **~85%** | 训练线在 A100 成熟栈上，风险已从 sm_120 移走 |

---

## 九、人工时间的真相

Claude Code 写代码不计时，但下面这些替不了，且是每天 8–10 小时的高强度：

| 项 | 小时 |
|---|---|
| **比对器黄金集 150 对裁定**（唯一完全不可外包） | 2.5 |
| 与官方脚本不一致 case 的人工复核 | 1 |
| 每轮读代码 + 答追问（19 轮 × 0.5h） | 9.5 |
| incident-log 根因撰写 | 1.5 |
| 架构图 + interview-qa + demo | 3 |
| 排障与决策（不可预估） | 5–10 |

**这是一个高强度冲刺，不是舒服的排期。进去之前你要知道。**

---

## 十、每日 build log

```
D[N] 2026-XX-XX
做了什么：
遇到什么问题：
怎么解决的：
测出什么数字：
```

训练相关额外记：配置 / 显存峰值 / 耗时 / 结果 / **踩过的坑**
（OOM 怎么解、NCCL 卡住怎么排查、loss 异常怎么定位、GDN 回退怎么发现的）。

**坑是面试里最值钱的素材。**
"我遇到过 X，现象是 Y，我用 Z 定位到根因是 W"比任何漂亮数字都更能证明你真做过。
